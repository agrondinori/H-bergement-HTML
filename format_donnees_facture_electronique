<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Capsule 3 — Formats et Mentions obligatoires</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700;800&amp;display=swap" rel="stylesheet" />
  <style>
    :root {
      --orisha-purple: #482882;
      --orisha-purple-light: #f3effa;
      --orisha-yellow: #ffbc00;
      --orisha-pink: #e8005f;
      --orisha-white: #ffffff;
      --orisha-black: #1a1a1a;
      --bg-gray: #e6e8ee;
      --text-gray: #4a5568;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background-color: var(--bg-gray);
      color: var(--orisha-black);
      padding: 30px 20px;
      display: flex;
      flex-direction: column;
      gap: 40px;
      align-items: center;
    }

    .slide {
      background: var(--orisha-white);
      width: 100%;
      max-width: 960px;
      min-height: 560px;
      border-radius: 24px;
      box-shadow: 0 12px 32px rgba(72, 40, 130, 0.12);
      padding: 28px 36px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      position: relative;
      overflow: hidden;
      scroll-margin-top: 30px;
    }

    .bg-wave {
      position: absolute;
      top: -40px;
      right: -40px;
      width: 300px;
      height: 300px;
      background: var(--orisha-purple-light);
      border-radius: 43% 57% 70% 30% / 30% 45% 55% 70%;
      z-index: 0;
    }

    .slide-content {
      position: relative;
      z-index: 1;
      height: 100%;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      flex: 1;
    }

    .slide-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 20px;
    }

    .slide-title {
      font-size: 1.4rem;
      font-weight: 800;
      color: var(--orisha-purple);
    }

    .slide-subtitle {
      font-size: 0.85rem;
      color: var(--text-gray);
      font-weight: 500;
      margin-top: 2px;
    }

    .badge-capsule {
      background-color: var(--orisha-purple-light);
      color: var(--orisha-purple);
      padding: 4px 14px;
      border-radius: 20px;
      font-weight: 700;
      font-size: 0.75rem;
    }

    /* DIAPO 1 : FACTURE INTERACTIVE */
    .interactive-grid {
      display: grid;
      grid-template-columns: 1.2fr 1fr;
      gap: 20px;
      margin: 10px 0;
      align-items: start;
    }

    .invoice-mockup {
      background: #fafafc;
      border: 1px solid #e2e8f0;
      border-radius: 12px;
      padding: 12px;
      font-family: monospace, sans-serif;
      font-size: 0.65rem;
      line-height: 1.3;
      color: #333;
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    .invoice-header {
      display: flex;
      justify-content: space-between;
      border-bottom: 2px solid var(--orisha-purple-light);
      padding-bottom: 4px;
    }

    .invoice-addresses {
      display: flex;
      justify-content: space-between;
      gap: 10px;
    }

    .address-box {
      background: #fff;
      padding: 6px;
      border: 1px solid #eee;
      border-radius: 6px;
      flex: 1;
    }

    .address-title {
      font-weight: bold;
      color: var(--orisha-purple);
      margin-bottom: 2px;
      font-size: 0.7rem;
    }

    .invoice-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 2px;
    }

    .invoice-table th, .invoice-table td {
      border: 1px solid #eee;
      padding: 4px;
      text-align: left;
    }

    .invoice-table th {
      background: var(--orisha-purple-light);
      color: var(--orisha-purple);
    }

    .hotspot-tag {
      display: inline-block;
      background: var(--orisha-yellow);
      color: var(--orisha-black);
      font-weight: 800;
      border-radius: 50%;
      width: 16px;
      height: 16px;
      text-align: center;
      line-height: 16px;
      font-size: 9px;
      cursor: pointer;
      margin-left: 4px;
    }

    .hotspot-tag.active {
      background: var(--orisha-pink);
      color: #fff;
    }

    .info-panel {
      background: var(--orisha-purple-light);
      border-radius: 12px;
      padding: 16px;
      display: flex;
      flex-direction: column;
      gap: 8px;
      border-left: 4px solid var(--orisha-purple);
    }

    .info-num {
      font-size: 0.9rem;
      font-weight: 800;
      color: var(--orisha-pink);
    }

    .info-title {
      font-size: 1.1rem;
      font-weight: 800;
      color: var(--orisha-purple);
    }

    .info-text {
      font-size: 0.8rem;
      line-height: 1.4;
      color: var(--orisha-black);
    }

    /* DIAPO 2 : FORMAT HYBRIDE */
    .hybrid-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin: 15px 0;
      flex: 1;
      align-items: center;
    }

    .hybrid-card {
      background: #fafafc;
      border: 2px solid var(--orisha-purple-light);
      border-radius: 16px;
      padding: 20px;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .hybrid-card.pink {
      border-color: #fdf5f8;
      background: #fdf5f8;
    }

    .hybrid-badge {
      font-size: 0.7rem;
      font-weight: 800;
      text-transform: uppercase;
      padding: 3px 10px;
      border-radius: 12px;
      width: fit-content;
    }

    .badge-human { background: var(--orisha-purple); color: #fff; }
    .badge-machine { background: var(--orisha-pink); color: #fff; }

    .hybrid-title {
      font-size: 1.1rem;
      font-weight: 800;
      color: var(--orisha-black);
    }

    .hybrid-desc {
      font-size: 0.82rem;
      line-height: 1.5;
      color: var(--text-gray);
    }

    /* DIAPO 3 : RISQUE DE REJET */
    .rejection-box {
      background: #fff0f5;
      border: 1px solid var(--orisha-pink);
      border-radius: 12px;
      padding: 15px 20px;
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .rejection-icon { font-size: 1.8rem; }
    .rejection-text { font-size: 0.82rem; color: var(--orisha-black); line-height: 1.4; }

    /* FOOTER */
    .slide-footer {
      display: grid;
      grid-template-columns: 1fr auto 1fr;
      align-items: center;
      font-size: 0.75rem;
      color: #777;
      font-weight: 600;
      border-top: 1px solid #eee;
      padding-top: 10px;
      margin-top: 15px;
    }

    .footer-right-group {
      display: flex;
      align-items: center;
      justify-content: flex-end;
      gap: 12px;
    }

    .footer-section-title {
      color: var(--orisha-purple);
      font-weight: 700;
      text-align: right;
    }

    .logo-footer {
      height: 24px;
      object-fit: contain;
    }

    .nav-btn {
      background-color: var(--orisha-pink);
      color: var(--orisha-white);
      border: none;
      padding: 5px 16px;
      border-radius: 20px;
      font-size: 0.75rem;
      font-weight: 700;
      cursor: pointer;
      transition: all 0.2s ease;
      display: flex;
      align-items: center;
      gap: 6px;
      box-shadow: 0 3px 8px rgba(232, 0, 95, 0.2);
    }

    .nav-btn:hover {
      background-color: var(--orisha-purple);
      transform: translateY(-1px);
    }
  </style>
</head>
<body>

  <!-- DIAPOSITIVE 1 : FACTURE INTERACTIVE -->
  <section class="slide" id="slide-1">
    <div class="bg-wave"></div>
    <div class="slide-content">
      
      <div class="slide-header">
        <div>
          <h1 class="slide-title">Les 4 nouvelles mentions obligatoires</h1>
          <p class="slide-subtitle">Cliquez sur les puces numérotées pour découvrir les données exigées.</p>
        </div>
        <div class="badge-capsule">Capsule 3 / 5</div>
      </div>

      <div class="interactive-grid">
        <div class="invoice-mockup">
          <div class="invoice-header">
            <div>
              <strong>ORISHA CONSTRUCTION</strong><br>
              Facture n° FA-2026-0901
            </div>
            <span>02/09/2026</span>
          </div>

          <div class="invoice-addresses">
            <div class="address-box">
              <div class="address-title">ÉMETTEUR</div>
              Orisha Construction<br>
              SIREN : 123 456 789
            </div>
            
            <div class="address-box">
              <div class="address-title">CLIENT</div>
              BATI-PRO SARL<br>
              SIREN : 987 654 321 <span class="hotspot-tag" id="tag-1" onclick="showInfo('siren')">1</span>
            </div>
          </div>

          <div class="address-box" style="width: 60%; align-self: flex-end;">
            <div class="address-title">LIVRAISON</div>
            Chantier Confluence, Lyon <span class="hotspot-tag" id="tag-2" onclick="showInfo('livraison')">2</span>
          </div>

          <table class="invoice-table">
            <thead>
              <tr><th>Description</th><th>Total HT</th></tr>
            </thead>
            <tbody>
              <tr><td>Matériel de chantier</td><td>500,00 €</td></tr>
              <tr><td>Prestation sur site</td><td>300,00 €</td></tr>
            </tbody>
          </table>

          <div style="background: #fdf5f8; padding: 4px; border-radius: 4px; font-weight: bold; font-size: 0.6rem;">
            Operation : Mixte (Biens &amp; Services) <span class="hotspot-tag" id="tag-3" onclick="showInfo('nature')">3</span>
          </div>

          <div style="align-self: flex-end; font-size: 0.6rem; color: #666;">
            TVA sur les débits <span class="hotspot-tag" id="tag-4" onclick="showInfo('tva')">4</span>
          </div>
        </div>

        <div class="info-panel" id="info-panel">
          <div class="info-num" id="info-num">1 / 4</div>
          <h3 class="info-title" id="info-title">Le SIREN du client</h3>
          <p class="info-text" id="info-text">Indispensable pour router la facture vers la bonne plateforme (PDP) dans l'annuaire national.</p>
        </div>
      </div>

      <div class="slide-footer">
        <span class="badge-capsule">Capsule 3 / 5</span>
        <button class="nav-btn" onclick="scrollToSlide('slide-2')">Diapositive suivante &rarr;</button>
        <div class="footer-right-group">
          <span class="footer-section-title">Anatomie de la facture</span>
          <img src="logo.png" alt="Orisha Construction" class="logo-footer" onerror="this.style.display='none'">
        </div>
      </div>

    </div>
  </section>

  <!-- DIAPOSITIVE 2 : FORMAT HYBRIDE -->
  <section class="slide" id="slide-2">
    <div class="bg-wave"></div>
    <div class="slide-content">
      
      <div class="slide-header">
        <div>
          <h1 class="slide-title">Le format Factur-X (PDF + XML)</h1>
          <p class="slide-subtitle">Comment ces données sont-elles transmises techniquement ?</p>
        </div>
        <div class="badge-capsule">Capsule 3 / 5</div>
      </div>

      <div class="hybrid-container">
        <div class="hybrid-card">
          <span class="hybrid-badge badge-human">Pour l'Humain</span>
          <h2 class="hybrid-title">Le Visuel PDF</h2>
          <p class="hybrid-desc">Affiche la facture de manière lisible pour les équipes comptables. Permet le contrôle visuel, la validation métier et l'archivage humain.</p>
        </div>

        <div class="hybrid-card pink">
          <span class="hybrid-badge badge-machine">Pour la Machine</span>
          <h2 class="hybrid-title">Le Fichier XML caché</h2>
          <p class="hybrid-desc">Contient les 4 nouvelles données obligatoires structurées. Il est lu automatiquement par la PDP et la DGFiP sans aucune saisie manuelle.</p>
        </div>
      </div>

      <div class="slide-footer">
        <span class="badge-capsule">Capsule 3 / 5</span>
        <button class="nav-btn" onclick="scrollToSlide('slide-3')">Diapositive suivante &rarr;</button>
        <div class="footer-right-group">
          <span class="footer-section-title">Factur-X</span>
          <img src="logo.png" alt="Orisha Construction" class="logo-footer" onerror="this.style.display='none'">
        </div>
      </div>

    </div>
  </section>

  <!-- DIAPOSITIVE 3 : IMPACTS ET REJET -->
  <section class="slide" id="slide-3">
    <div class="bg-wave"></div>
    <div class="slide-content">
      
      <div class="slide-header">
        <div>
          <h1 class="slide-title">Risques et points de vigilance</h1>
          <p class="slide-subtitle">Ce que cela change au quotidien pour les équipes d'Orisha Construction.</p>
        </div>
        <div class="badge-capsule">Capsule 3 / 5</div>
      </div>

      <div class="rejection-box">
        <div class="rejection-icon">⚠️</div>
        <div class="rejection-text">
          <strong>Rejet Automatique :</strong> Si l'une de ces 4 mentions est absente du fichier XML, la plateforme (PDP) rejette la facture instantanément. Elle n'arrive jamais chez le client.
        </div>
      </div>

      <div class="hybrid-card">
        <h2 class="hybrid-title">Les réflexes à adopter dans l'ERP</h2>
        <p class="hybrid-desc">
          • <strong>Compléter les fiches tiers :</strong> Renseigner systématiquement le SIREN des clients professionnels.<br>
          • <strong>Renseigner le chantier :</strong> Indiquer l'adresse de livraison exacte si elle diffère du siège.<br>
          • <strong>Catégoriser les lignes :</strong> Distinguer les ventes de matériel (Biens) et les prestations de pose (Services).
        </p>
      </div>

      <div class="slide-footer">
        <span class="badge-capsule">Capsule 3 / 5</span>
        <button class="nav-btn" onclick="scrollToSlide('slide-1')">&circlearrowright; Recommencer</button>
        <div class="footer-right-group">
          <span class="footer-section-title">Impacts Métier</span>
          <img src="logo.png" alt="Orisha Construction" class="logo-footer" onerror="this.style.display='none'">
        </div>
      </div>

    </div>
  </section>

  <script>
    const infos = {
      'siren': { num: "1 / 4", title: "Le SIREN du client", text: "Code obligatoire pour identifier le destinataire dans l'annuaire central et router la facture vers sa plateforme." },
      'livraison': { num: "2 / 4", title: "L'adresse de livraison", text: "Requise si différente du siège. Elle permet à l'État de contrôler la territorialité fiscale de la TVA." },
      'nature': { num: "3 / 4", title: "Nature de l'opération", text: "Distingue les Biens, les Services ou les opérations Mixtes pour fixer la date d'exigibilité de la TVA." },
      'tva': { num: "4 / 4", title: "Option sur les débits", text: "Obligatoire si vous déclarez la TVA dès l'émission de la facture au lieu de l'encaissement." }
    };

    function showInfo(id) {
      document.querySelectorAll('.hotspot-tag').forEach(t => t.classList.remove('active'));
      
      const tagMap = {'siren': 'tag-1', 'livraison': 'tag-2', 'nature': 'tag-3', 'tva': 'tag-4'};
      document.getElementById(tagMap[id]).classList.add('active');

      document.getElementById('info-num').innerText = infos[id].num;
      document.getElementById('info-title').innerText = infos[id].title;
      document.getElementById('info-text').innerText = infos[id].text;
    }

    window.onload = () => { showInfo('siren'); };

    function scrollToSlide(slideId) {
      document.getElementById(slideId).scrollIntoView({ behavior: 'smooth' });
    }
  </script>

</body>
</html>
